# 网卡智能关机助手 V1.6

V1.6 新增多显卡 GPU 监控与 GPU 低负载自动关机。

## 新增

- 自动检测本机显卡数量与显卡名称。
- 实时显示每张显卡 GPU 使用率。
- 实时显示每张显卡专用显存占用、总显存与显存使用率。
- 可指定其中一张显卡作为自动关机判断对象。
- 可分别设置 GPU 使用率阈值和显存使用率阈值。
- 判断逻辑为 OR：GPU 使用率或显存使用率任意一项低于阈值即可开始计时。
- 可设置低负载条件连续多少秒后关机。
- 条件恢复后自动清零计时；GPU 数据丢失时暂停计时。

## 技术实现

- DXGI：枚举 GPU、名称、LUID、专用显存总量。
- Windows PDH：采集 GPU Engine 与 GPU Adapter Memory 性能数据。
- 无第三方 Go 依赖，仍可使用纯 Go 交叉编译 Windows x64。

## 保留

- 倒计时关机
- 指定时间关机
- 总网速低于/高于阈值关机
- 自动撤销已排定关机
- 设置保存与日志
